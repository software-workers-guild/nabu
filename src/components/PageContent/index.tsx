import * as React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  border: 1px solid #000;
`;

const PageContent = () => <Wrapper>page content</Wrapper>;

export default PageContent;
